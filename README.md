* Add your own metric in prometheus.yml in target config
* Start up prometheus + grafana: 
     ```bash
     docker-compose up -d
     ```
* Check that your target is up http://localhost:9090/targets
* Grafana user/pass by default is ``admin`` 